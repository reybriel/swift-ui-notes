enum Strings {
    enum NoteList {
        static let pageTitle: String = "Notes"
        static let rightBarButton: String = "Add"
        static let emptyContentPlaceholder: String = "..."
    }
    enum NoteCreate {
        static let newTitleMessage: String = "Add a title for yout note"
    }
}
